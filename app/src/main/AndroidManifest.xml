<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fq.inpaokeuse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ZhiHuHeadActivity"></activity>
        <activity android:name=".activity.AnimationActivity"></activity>
        <activity android:name=".activity.Animation3DActivity"></activity>
        <activity android:name=".activity.ContactsListActivity"></activity>
        <activity android:name=".activity.DefineScaleActivity"></activity>
        <activity android:name=".activity.GlideTestActivity"></activity>
        <activity android:name=".activity.UniversalImageLoaderTestActivity" />
        <activity android:name=".activity.TouTiaoHeadActivity" />
        <activity android:name=".activity.CameraActivity" />
    </application>

</manifest>